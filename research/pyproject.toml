[project]
name = "research"
version = "0.1.0"
description = "Citation compliance research for AI search providers"
requires-python = ">=3.13"
dependencies = [
    "openai",
    "google-genai",
    "perplexityai",
    "requests",
    "pandas",
    "python-dotenv>=1.2.1",
]
